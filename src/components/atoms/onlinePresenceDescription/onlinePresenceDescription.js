import React from "react";
import "./onlinePresenceDescription.css";

export default function OnlinePresenceDescription({ description }) {
    return <p className="onlinePresenceDescription">{description}</p>
}