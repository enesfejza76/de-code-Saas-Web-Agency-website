import React from "react";
import "./serviceHeaderDescription.css";

export default function ServiceHeaderDescription({ description }) {
    return <p className="serviceHeaderDescription">{description}</p>
}